{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "hive": {
      "type": "local",
      "command": ["uv", "run", "--extra", "mcp", "hive-mcp"],
      "enabled": true,
      "environment": {
        "HIVE_BASE_PATH": "."
      }
    }
  }
}
