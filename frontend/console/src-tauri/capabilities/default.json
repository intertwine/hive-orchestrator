{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables only the default core capability for the beta shell",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
